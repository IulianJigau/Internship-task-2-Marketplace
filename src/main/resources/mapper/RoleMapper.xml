<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java.test.junior.mapper.RoleMapper">

    <select id="exists" parameterType="Integer">
        SELECT EXISTS
                   (SELECT 1
                    FROM role
                    WHERE id = #{id});
    </select>

    <select id="existsName" parameterType="String">
        SELECT EXISTS
                   (SELECT 1
                    FROM role
                    WHERE name = UPPER(#{name}));
    </select>

    <select id="find" parameterType="Integer" resultMap="roleResultMapper">
        SELECT *
        FROM role
        WHERE id = #{id}
    </select>

    <select id="findByName" parameterType="String" resultMap="roleResultMapper">
        SELECT *
        FROM role
        WHERE name = UPPER(#{name})
    </select>


    <select id="findAll" resultMap="roleResultMapper">
        SELECT *
        FROM role
    </select>

    <insert id="insert"
            parameterType="String">
        INSERT INTO role (name)
        VALUES (UPPER(#{name}))
    </insert>

    <delete id="delete" parameterType="Integer">
        DELETE
        FROM role
        WHERE id = #{id}
    </delete>

    <select id="existsUserRole" parameterType="map">
        SELECT EXISTS
                   (SELECT 1
                    FROM user_role
                    WHERE user_id = #{userId}
                      AND role_id = #{roleId});
    </select>

    <select id="findUserRoles" parameterType="Long" resultType="String">
        SELECT role.name AS role_name
        FROM role
                 JOIN user_role ON user_role.role_id = role.id
        WHERE user_role.user_id = #{userId};
    </select>

    <insert id="insertUserRole" parameterType="map">
        INSERT INTO user_role (user_id, role_id)
        VALUES (#{userId}, #{roleId})
    </insert>

    <delete id="deleteUserRole" parameterType="map">
        DELETE
        FROM user_role
        WHERE user_id = #{userId}
          AND role_id = #{roleId}
    </delete>

    <resultMap id="roleResultMapper" type="com.java.test.junior.model.Role">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
    </resultMap>
</mapper>
