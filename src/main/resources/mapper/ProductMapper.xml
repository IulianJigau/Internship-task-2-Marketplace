<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java.test.junior.mapper.ProductMapper">

    <select id="findById" parameterType="Long" resultMap="productResultMapper">
        SELECT *
        FROM product
        WHERE id = #{id}
    </select>

    <select id="getPage" parameterType="map" resultMap="productResultMapper">
        SELECT *
        FROM product
        <where>
            <if test="query != null and query != ''">
                fts_vector @@ plainto_tsquery('english', #{query})
            </if>
        </where>
        ORDER BY id
        LIMIT #{size}
        OFFSET #{size} * (#{page} - 1)
    </select>

    <insert id="insert"
            parameterType="com.java.test.junior.model.ProductDTO"
            useGeneratedKeys="true"
            keyColumn="id">
        INSERT INTO product (name, price, description, user_id)
        VALUES (#{name}, #{price}, #{description}, 1)
    </insert>

    <update id="update" parameterType="map">
        UPDATE product
        SET name        = #{product.name},
            price       = #{product.price},
            description = #{product.description},
            updated_at  = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE
        FROM product
        WHERE id = #{id}
    </delete>

    <resultMap id="productResultMapper" type="com.java.test.junior.model.Product">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="price" column="price"/>
        <result property="description" column="description"/>
        <result property="userId" column="user_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

</mapper>
